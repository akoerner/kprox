// Global Keyboard and Mouse instances are defined in kprox.ino (with device
// name/manufacturer/battery constructor args). This file is intentionally empty
// to avoid duplicate symbol errors when the library is compiled as part of the
// main source tree rather than as an external dependency.
#include "BleCombo.h"
